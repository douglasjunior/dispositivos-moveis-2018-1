<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    tools:context=".MainActivity">

    <EditText
        android:id="@+id/et_mensagem"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/et_mensagem_hint" />

    <Button
        android:id="@+id/btn_enviar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_enviar" />

    <Button
        android:id="@+id/btn_abrir_site"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_abrir_site"
        android:onClick="abrirSite"/>

    <Button
        android:id="@+id/btn_abrir_mapa"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/btn_abrir_mapa"
        android:onClick="abrirMapa"/>

</LinearLayout>
